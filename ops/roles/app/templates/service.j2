[Unit]
Description=Rewind Backend
After=network.target

[Service]
ExecStart={{ app_dir }}/{{ current_name }}/bin/rewind -Dlogback.configurationFile={{ conf_dir }}/logback-{{ current_name }}.xml
Restart=on-failure
SuccessExitStatus=143

[Install]
WantedBy=default.target

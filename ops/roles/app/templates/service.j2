[Unit]
Description=Dividat Backend
After=network.target

[Service]
ExecStart={{ app_dir }}/{{ current_name }}/bin/dividat-backend -Dlogback.configurationFile={{ app_dir }}/logback-{{ current_name }}.xml
Restart=on-failure
SuccessExitStatus=143

[Install]
WantedBy=default.target

http {
    port = 9060
    address = "127.0.0.1"
}
db {
    uri = "jdbc:postgresql://localhost/rewind-dev"
    user = "rewind"
}

grib-storage {
    region = "eu-west-3"
    bucket = "rewind-gribs"
}

include "secrets.conf"
